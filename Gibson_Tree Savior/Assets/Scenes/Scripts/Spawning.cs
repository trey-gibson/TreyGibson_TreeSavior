using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Spawning : MonoBehaviour {

	// Use this for initialization
	void Start () {
        for (int i = 0; i < 5; i++)
        {
            Instantiate(GameObject)TreeTrunk)
        }
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
